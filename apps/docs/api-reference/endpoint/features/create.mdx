---
title: 'Create a Feature'
api: 'POST https://api.eververse.ai/feature'
---

<ParamField body="title" type="string" required>
  The title of the feature e.g `Make the logo bigger`
</ParamField>

<ParamField body="text" type="string" required>
  The text of the feature e.g `The logo is too small`
</ParamField>

<ParamField body="custom" type="object[]">
  <Expandable title="properties">
    <ParamField body="key" type="string" required>
      The key of the custom field e.g `priority`
    </ParamField>

    <ParamField body="value" type="string" required>
      The value of the custom field e.g `high`
    </ParamField>
  </Expandable>
</ParamField>
