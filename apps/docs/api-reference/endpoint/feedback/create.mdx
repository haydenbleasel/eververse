---
title: 'Create Feedback'
api: 'POST https://api.eververse.ai/feedback'
---

<ParamField body="title" type="string" required>
  The title of the feedback e.g `Make the logo bigger`
</ParamField>

<ParamField body="text" type="string" required>
  The text of the feedback e.g `The logo is too small`
</ParamField>

<ParamField body="user" type="object">
  <Expandable title="properties">
    <ParamField body="name" type="string" required>
      The name of the user e.g `John Doe`
    </ParamField>

    <ParamField body="email" type="string" required>
      The email of the user e.g `john@acme.com`
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="organization" type="object">
  <Expandable title="properties">
    <ParamField body="name" type="string" required>
      The name of the organization e.g `Acme Inc`
    </ParamField>

    <ParamField body="domain" type="string" required>
      The domain of the organization e.g `acme.com`
    </ParamField>
  </Expandable>
</ParamField>